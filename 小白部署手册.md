# 🍍 一鸣鲜果 - 小白部署手册

> 写给完全不懂技术的人，一步一步照着做就行。
> 遇到问题直接复制报错信息问 Claude Code。

---

## 📁 你的项目在哪里

```
/Users/yiming/雷一鸣副业/fruit-store/
├── server/          ← 后端（API接口+数据库）
│   └── fruit.db     ← 这是你的数据库文件，所有数据都在这里
├── admin/           ← 商家后台网页源码
│   └── dist/        ← 打包后的后台网页（发给用户访问的）
├── web/             ← 手机H5商城源码
│   └── dist/        ← 打包后的商城网页（发给用户访问的）
├── start.sh         ← 一键启动脚本
└── 小白部署手册.md   ← 你现在看的这个文件
```

---

## 🚀 第一步：启动服务（每天开店第一件事）

打开终端（Mac 上的"终端"应用），输入以下命令：

```bash
cd /Users/yiming/雷一鸣副业/fruit-store
bash start.sh
```

看到以下输出就说明启动成功了：

```
🍉 ====================================
🍍 一鸣鲜果 后端服务已启动
🍊 服务地址: http://localhost:3456
📊 商家后台: http://localhost:3456/admin
📱 用户商城: http://localhost:3456/web
🍉 ====================================
```

**不要关掉这个终端窗口！** 关掉服务就停了。

关闭方法：在终端里按 `Ctrl + C`。

---

## 📱 第二步：把商城链接发给微信群

### 在你自己的电脑上测试

打开浏览器，访问：
- **手机商城**：http://localhost:3456/web
- **商家后台**：http://localhost:3456/admin

### 让微信群里的客户也能访问（需要部署到公网）

你的电脑在家，微信群友在外网，访问不到你的 `localhost`。需要把服务部署到公网，有以下几种方案：

---

## 🌐 方案一：免费方案 - Render 部署（推荐起步用）

> 优点：免费、不需要备案、不需要买服务器
> 缺点：免费版15分钟没人访问会休眠（唤醒需要几秒）

### 步骤：

**1. 注册 Render 账号**
- 打开 https://render.com
- 点 "Get Started" → 用 GitHub 账号登录（没有 GitHub 的话先注册一个）
- 如果不想用 GitHub，也可以用邮箱注册

**2. 上传代码到 GitHub**
- 打开 https://github.com → 登录 → 点右上角 "+" → "New repository"
- Repository name 填 `fruit-store`
- 选 "Private"（私有的，别人看不到你的代码）
- 不要勾选任何初始化选项
- 点 "Create repository"

**3. 在终端上传代码**

打开终端，逐行执行：

```bash
# 进入项目目录
cd /Users/yiming/雷一鸣副业/fruit-store

# 初始化git
git init
git add .
git commit -m "一鸣鲜果初始版本"

# 关联你的GitHub仓库（把下面地址换成你自己的）
git remote add origin https://github.com/你的GitHub用户名/fruit-store.git

# 推送代码
git branch -M main
git push -u origin main
```

**4. 在 Render 创建服务**
- 打开 https://dashboard.render.com
- 点 "New +" → "Web Service"
- 连接你的 GitHub 仓库 fruit-store
- 填写以下配置：

```
Name: yiming-fruit
Runtime: Node
Build Command: cd server && npm install
Start Command: cd server && node index.js
```

- 点 "Create Web Service"
- 等待部署完成（约3-5分钟）
- Render 会给你一个免费域名，像这样：`https://yiming-fruit.onrender.com`

**5. 测试**
- 手机商城：`https://你的域名.onrender.com/web`
- 商家后台：`https://你的域名.onrender.com/admin`
- 把这个链接发到微信群里！

> ⚠️ 免费版会休眠：如果15分钟没人访问，下次打开需要等30秒左右才能加载出来。
> 如果不想休眠，可以用方案二。

---

## 💰 方案二：国内服务器（更稳定，约50元/月）

> 优点：国内访问快、不用翻墙、稳定不关停
> 缺点：需要备案、需要花钱

### 购买服务器

**推荐：阿里云 ECS 或 腾讯云轻量应用服务器**
- 最低配置：1核2G，约50元/月
- 系统选 CentOS 7.9 或 Ubuntu 22.04
- 地域选北京（离乌兰察布近）
- 买完后记住 IP 地址和 root 密码

### 连接服务器

打开终端，输入：
```bash
ssh root@你的服务器IP
```
输入密码（输入时看不到是正常的），回车。

### 在服务器上安装环境

逐行执行：
```bash
# 更新系统
apt update && apt upgrade -y

# 安装 Node.js
curl -fsSL https://deb.nodesource.com/setup_22.x | bash -
apt install -y nodejs

# 验证安装
node -v
npm -v
```

### 上传代码到服务器

回到你 Mac 的终端（新开一个窗口）：

```bash
# 把整个项目传到服务器
cd /Users/yiming/雷一鸣副业
scp -r fruit-store root@你的服务器IP:/root/
```

### 在服务器上启动

回到服务器终端：
```bash
cd /root/fruit-store/server
npm install
npm run seed    # 初始化数据库
node index.js &  # 后台运行
```

### 配置域名（可选）

1. 在阿里云/腾讯云买一个域名（约50元/年），比如 `yimingfruit.cn`
2. 在域名管理里添加 A 记录，指向你的服务器 IP
3. 备案（按平台指引操作，约1-2周）

> 💡 买域名的事不急，先用 IP 地址访问就行。

---

## 🔒 方案三：内网穿透（临时分享用）

> 适合：临时给朋友看、不想部署服务器

使用 ngrok（免费，国外服务，需要开 VPN）：

```bash
# 安装 ngrok（只需要一次）
brew install ngrok

# 启动内网穿透
ngrok http 3456
```

ngrok 会给你一个公网链接，像这样：`https://xxxx.ngrok.io`
把这个链接发到微信群即可。

> ⚠️ 关掉终端就失效了，链接每次启动都会变。

---

## 📝 日常操作指南

### 修改水果信息（上架/改价/改库存）

1. 浏览器打开商家后台 → 登录（admin/123456）
2. 点左边菜单「商品管理」
3. 看到所有水果列表，点「编辑」就能改
4. 开关控制上架/下架

### 查看今天卖了多少

1. 打开商家后台 → 「数据大屏」
2. 看顶部的统计卡片：今日订单数、销售额、下单人数
3. 往下滑看图表

### 处理订单

1. 打开商家后台 → 「订单管理」
2. 看到新订单 → 点「确认备货」
3. 货到了 → 点「到货通知」
4. 客户取走 → 点「已自提」

### 备份数据（重要！定期做）

你的数据库文件在这里：
```
/Users/yiming/雷一鸣副业/fruit-store/server/fruit.db
```

**备份方法**：直接把这个文件复制到安全的地方（U盘、iCloud、百度网盘等）

建议每周备份一次：
```bash
cp /Users/yiming/雷一鸣副业/fruit-store/server/fruit.db ~/Desktop/备份_fruit_$(date +%Y%m%d).db
```

---

## 🔑 账号密码

| 用途 | 用户名 | 密码 | 备注 |
|------|--------|------|------|
| 老板 | admin | 123456 | 最高权限，管理所有 |
| 合伙人 | partner | 123456 | 运营权限 |

**⚠️ 上线前务必改密码！** 在终端执行：

```bash
cd /Users/yiming/雷一鸣副业/fruit-store/server
node -e "
const {db} = require('./db');
const crypto = require('crypto');
const hash = crypto.createHash('sha256').update('你的新密码').digest('hex');
db.prepare('UPDATE admins SET password_hash = ? WHERE username = ?').run(hash, 'admin');
console.log('admin密码已修改');
"
```

---

## 🐛 常见问题

### Q: 启动报错 "command not found: node"
A: Node.js 没装好，执行 `brew install node@22`

### Q: 页面打不开
A: 检查终端里的服务是不是还在运行，关了就要重新 `bash start.sh`

### Q: 后台图表不显示
A: 可能没有订单数据。先用手机端下几单测试。

### Q: 想改自提点地址
A: 打开后台 → 订单管理 → 在网页上改。代码里的默认地址在 `web/src/views/Checkout.vue` 第22行。

### Q: 图片不显示
A: Unsplash 的图片有时被墙。可以换图片URL，或者用本地图片（放在 server/uploads 目录下）。

---

## 📞 需要帮助？

打开 Claude Code，把报错信息贴进去，我来帮你解决。
